type: object
required:
  - chain_id
  - address
  - token_id
properties:
  address:
    type: string
  chain_id:
    type: string
  token_id:
    type: string
  amount:
    type: string
    format: amount.Amount
    description: amount of token to approve